import SwiftUI

struct ShareView: View {
    var body: some View {
        VStack(spacing: 12) {
            Text("Crotchet Share Extension")
                .font(.headline)
            Text("Payload normalization will be added later.")
                .font(.subheadline)
                .foregroundStyle(.secondary)
        }
        .padding(16)
    }
}
