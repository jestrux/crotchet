import Widget from "../components/Widget";

const UserStatusWidget = () => {
	return (
		<Widget>
			<div className="h-full text-sm opacity-50 flex items-center justify-center">
				User status
			</div>
		</Widget>
	);
};

export default UserStatusWidget;
